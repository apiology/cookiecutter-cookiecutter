---
# https://yamllint.readthedocs.io/en/stable/configuration.html
extends: default

rules:
  comments: disable
  line-length:
    max: 120
    ignore: |
      **/.circleci/config.yml
  indentation:
    ignore: |
      rbs_collection.lock.yaml
